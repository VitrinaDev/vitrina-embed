{
  "name": "@vitrina/embed-workspace",
  "version": "0.0.0",
  "private": true,
  "description": "Vitrina embeddable web SDK: chat widget + lead form + stock client for dealer websites.",
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@11.1.3",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  }
}
